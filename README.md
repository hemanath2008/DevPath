# CodeEasy

## Setup

1. Copy `.env.example` to `.env.local` and set the Supabase URL and anon key.
2. Run the SQL in `supabase/schema.sql` in Supabase SQL Editor.
3. Install dependencies:

```bash
npm install
```

4. Start the app:

```bash
npm run dev
```